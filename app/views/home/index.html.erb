<div class="text-center">
  <div class="input-group d-flex justify-content-center my-3">
    <%= form_with(url: search_auctions_path, method: :get) do |f| %>
      <%= f.label :query, 'Buscar Leilões', class: 'h2' %>
      <div class="d-flex">
        <%= f.text_field :query, class: 'form-control mx-2' %>
        <%= f.submit 'Buscar', class: 'btn btn-primary' %>
      </div>
    <% end %>
  </div>
  <h2>Lotes em Andamento</h2>
  <% @in_progress.each do |i| %>
    <div>
      <%= link_to i.code, auction_path(i.id) %>
    <div>
  <% end %>

  <h2>Lotes Futuros</h2>
  <% @future.each do |f| %>
    <div>
      <%= link_to f.code, auction_path(f.id) %>
    </div>
  <% end %>
</div>